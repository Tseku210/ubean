---

---

<div class="grid grid-cols-2 items-center gap-x-8 gap-y-16 md:grid-cols-4">
  {
    Array.from({ length: 4 }).map((_) => (
      <div class="h-60 w-48 animate-pulse place-self-center rounded-4xl bg-gray-200" />
    ))
  }
</div>
