---
import type { SanityDocument } from "@sanity/client";
import { formatPrice } from "@/lib/utils";
import SanityImage from "../SanityImage.astro";

interface Props {
  menuItem: SanityDocument;
}

const { menuItem } = Astro.props;
---

<div class="relative w-fit space-y-4 group">
  <div
    class="shadow-card absolute bottom-0 -z-1 m-0 h-3/6 w-full rounded-4xl bg-white"
  >
  </div>
  <div class="transition-transform group-hover:-translate-y-2">
    <SanityImage
      node={menuItem.image}
      alt={`${menuItem.name} menu item`}
      width={270}
    />
  </div>
  <div class="flex flex-col space-y-2 px-6 pb-7 text-left">
    <span class="text-h6">{menuItem.name}</span>
    <span class="text-h6">{formatPrice(menuItem.price)}</span>
  </div>
</div>
